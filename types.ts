export interface User {
  email: string;
  password: string;
}
// This file defines the User interface, which is making a new type
// the type User has two structures, email and password both of type string.
